/**
 *  GIVEN REQUIREMENTS
 *
 *  Locations = (x, y)
 *  Distance between two points = sqrt((x2-x1)*(x2-x1) + (y2-y1)*(y2-y1))
 *  Threshold = Can assume a number above which a cab will not be shown
 *
 *  Only one type of cab is there.
 *  No cab sharing is allowed; 1:1 relation with cab : rider
 *
 */
